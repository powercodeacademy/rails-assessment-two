<p id="vendor-info"><%= notice %></p>

<p>
  <h3>Name:</h3>
  <%= @vendor.name %>
<h3>What we sell:</h3>
  <ul>
  <% @vendor.sweets.each do |sweet| %>
    <li><%= link_to sweet.name, sweet %></li>
  <% end  %>
</ul>

<%= render 'form' %>
