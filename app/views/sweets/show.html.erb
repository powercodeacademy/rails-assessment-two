<h1><%= @sweet.name %></h1>

<p><%= link_to "Back to all sweets", sweets_path %></p>

<h2>Vendors that sell this sweet</h2>
<ul>
  <% @sweet.vendors.each do |vendor| %>
    <li><%= link_to vendor.name, vendor_path(vendor) %></li>
  <% end %>
</ul>
